	    	<%= link_to like_post_path(post), remote: true, method: :put, id: dom_id(post), class:'lightublb' do %>
	    		<i class="fa fa-lightbulb-o"></i>
	    	<%end%>
	    	<%= post.get_upvotes.size %>
