<div class='signup-form-title'>
  <%= nav_link_to 'Log in' ,new_user_session_path, class: 'login-header-form login-a' %>
  <%= nav_link_to 'Sign up' ,new_user_registration_path, class: 'login-header-form login-b' %>
</div>

<div class="form">

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.text_field :username, autofocus: true , placeholder: 'Username', class: 'username-field input' %>
  </div>

  <div class="field">
    <%= f.email_field :email , placeholder: 'Email', class: 'email-field input' %>
  </div>

  <div class="field">
    <%= f.password_field :password, autocomplete: "off", placeholder: 'Password (8 characters minimum)', class: 'password-field input'  %>
  </div>

  <div class="field">
    <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: 'Password Confirmation', class: 'password-field input'  %>
  </div>

  <div class="actions">
    <%= f.submit "Sign up" , class: 'signup-submit submit' %>
  </div>
<% end %>

</div>
