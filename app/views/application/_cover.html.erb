<div class="hero-cover">
            <% if current_page?('/') or current_page?('/posts') %>
                <div class="img-cover"></div>
                <%= image_tag('cover-photo-min', class: 'cover-photo-img') %> 
                  <div class="hero-title">
                    <h1><%= @category_title %> </h1>
                    <h3><%= @category_slogan %> </h3>
                  </div>

                  <div class="hero-buttons center">
                        <div class="mostrecent-button" onclick="window.location.reload(true)">Latest Videos</div>
                        <%= link_to 'Randomize' , randomize_posts_path , id: 'random-button' %>
                  </div>


                  <div class="start-watching center">Start watching now</div>
                  <div class="scroll-to-content">
                        <i class="fa fa-angle-double-down scroll-arrow"></i>
                  </div>
            <% elsif current_page?('/profile') %>
                <div class="img-cover"></div>
                <%= image_tag('blurred-cover-photo', class: 'cover-photo-img') %> 
            <% else %>
              <%= link_to root_path do %>
                <div class="img-cover"></div>
                <%= image_tag('blurred-cover-photo' ,class: 'cover-photo-img') %> 
              <% end %>
           <% end %>

          
</div>

